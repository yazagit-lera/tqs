#!/data/data/com.termux/files/usr/bin/env bash
#
# Openssh helper
set -e

if [[ "${@}" =~ '-h' ]]; then
  cat << 'EOF'
- (Re)start `sshd`
  Use `k` option to kill server
- Example:
  op k
EOF

  exit 0
fi

pidof -q sshd && pkill sshd
[[ "${1}" != 'k' ]] && sshd -p8022
